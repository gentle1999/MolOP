# molop.unit

This page provides the API reference for the `molop.unit` module.

Use `unit_transform` for explicit quantity conversion. `si_ureg` and
`atom_ureg` are the registries used by MolOP for SI and atomic-unit quantities;
keep the quantity object when unit metadata matters.

::: molop.unit
