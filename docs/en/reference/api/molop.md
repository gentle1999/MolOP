# molop

This page provides the API reference for the `molop` module.

The stable top-level exports are `AutoParser`, `molopconfig`, `moloplogger`,
and `__version__`. Start with the [Python API guide](../../getting_started/python-api.md)
for parsing workflows.

::: molop
    options:
      filters:
        - "!AutoParser"
